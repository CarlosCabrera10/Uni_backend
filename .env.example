# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Server
PORT=3000

# Optional: if you use a separate database connection for direct PG access
#DATABASE_URL=postgresql://user:pass@dbhost:5432/dbname

# IMPORTANT: keep service_role key secret and never commit real keys